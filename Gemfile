source 'https://rubygems.org'

# Specify your gem's dependencies in htwax.gemspec
gemspec

# Things that we use, but which the code doesn't depend on.
# Helps Travis-CI to not install so much.
group :tools do
  gem 'rake'
  gem 'guard-minitest'
end
